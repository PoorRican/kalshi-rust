[package]
name = "sample_bot"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
kalshi = { path = "../kalshi" }  # TODO: update with new crate or GitHub link
dotenv = "0.15"
